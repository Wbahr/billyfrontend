import React, { useState } from 'react'
import styled from 'styled-components'
import _ from 'lodash'
// import { Modal } from 'react-responsive-modal'

export default function DetailsModal({showDetailsModal, toggleDetailsModal}) {

  return(
    // <Modal open={showDetailsModal} onClose={()=>{toggleDetailsModal()}} showCloseIcon={false} closeOnOverlayClick={false} center>
    //   <p>Product Details Here</p>
    // </Modal>
    <>
    </>
  )
}