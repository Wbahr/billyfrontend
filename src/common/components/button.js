import React from "react"

class Button extends React.Component {

  render(){
    const {
      name
    } = this.props

    return(
      <button>

      </button>
    )
  }
}

export default Button
